[package]
cjc-version = "1.0.5"
name = "bench_micro"
version = "0.1.0"
output-type = "executable"
description = "Headless single-scenario micro-benchmarks for CangHui."

[dependencies]
chui = { path = "../.." }
bench_harness = { path = "../harness" }
bench_scenes = { path = "../scenes" }
