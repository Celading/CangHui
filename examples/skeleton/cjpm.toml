[package]
cjc-version = "1.0.5"
name = "chui_skeleton"
version = "0.1.0"
output-type = "executable"
description = "CangHui Pulse — a breathing skeleton loading screen that swaps to real content."

[dependencies]
chui = { path = "../.." }
