[package]
cjc-version = "1.0.5"
name = "canghui_mobile_host_replay"
version = "0.1.0"
output-type = "executable"
description = "Headless consumer fixture for CangHui mobile host receipts."

[dependencies]
chui = { path = "../.." }
