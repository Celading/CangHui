[package]
cjc-version = "1.0.5"
name = "chui_shadows"
version = "0.1.0"
output-type = "executable"
description = "CangHui layered shadows — Material key+ambient dual layer vs single layer, side by side."

[dependencies]
chui = { path = "../.." }
