[package]
cjc-version = "1.0.5"
name = "chui_cards"
version = "0.1.0"
output-type = "executable"
description = "CangHui style modifiers (shadow elevation, corner radius, border) card playground."

[dependencies]
chui = { path = "../.." }
