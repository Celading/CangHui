[package]
cjc-version = "1.0.5"
name = "chui_motion"
version = "0.1.0"
output-type = "executable"
description = "CangHui easing curves + Animator motion lab (parallel eased tracks)."

[dependencies]
chui = { path = "../.." }
